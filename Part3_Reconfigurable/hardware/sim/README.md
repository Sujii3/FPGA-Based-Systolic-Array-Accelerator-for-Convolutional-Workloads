#### How to run this test bench?

* Make sure you are in the `Part3_Reconfigurable/hardware/sim` directory
* Run `iverilog -o mac_sim -f filelist`
* Run `vvp mac_sim`

This will generate the vcd file and an output on your terminal